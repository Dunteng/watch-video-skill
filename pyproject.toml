[project]
name = "watchvideo"
version = "0.1.0"
description = "Analyze local or remote videos with yt-dlp, ffmpeg, subtitles, transcription hooks, and keyframes."
requires-python = ">=3.11"

[project.scripts]
watchvideo = "watchvideo.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
